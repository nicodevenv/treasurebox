version: "2"
services:
  www:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - /var/www/html/vendor/
    environment:
      WEB_DOCUMENT_ROOT: "/var/www/html/public"
      APP_ENV: ${APP_ENV}